source "https://rubygems.org"

gemspec path: '..'

gem "rails", "~> 6.1.0"
gem "rack", ">= 1.4.5"

case ENV["DB"]
when "postgresql"
  gem "pg"
when "mysql"
  gem "mysql2"
when "sqlite"
  # Rails 6.1 / AR 6.1 requires sqlite3 1.4.x
  gem "sqlite3", "~> 1.4"
else
  gem "sqlite3"
end
